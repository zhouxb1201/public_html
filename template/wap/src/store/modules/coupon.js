// import { GET_shopCoupon, RECEIVE_COUPON } from "@/api/member";
const coupon = {
  state: {},
  mutations: {},
  actions: {
    // 获取店铺优惠券列表
    // getShopCoupon({ commit }, shop_id) {
    //   return new Promise((resolve, reject) => {
    //     GET_shopCoupon(shop_id).then(res => {
    //       resolve(res)
    //     }).catch(error => {
    //       reject(error)
    //     })
    //   })
    // },
    // 领取优惠券

  }
}

export default coupon
